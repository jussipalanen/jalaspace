# Copy to .env.local to override locally. Never commit real secrets.

# Persistence implementation: localStorage (default) or api (planned, not implemented yet).
VITE_DATA_PROVIDER=localStorage

# Base URL of the JalaSpace API. Unset: features that need the API, such as AI
# maintenance suggestions, are not offered.
# VITE_API_URL=http://localhost:3000
